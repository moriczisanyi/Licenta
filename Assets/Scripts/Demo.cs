using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class Demo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Transform agentTransform = transform.Find("RLAgent");
        //RLAgent agent = agentTransform.GetComponent<RLAgent>();
        // agent.gameObject.SetActive(true);
        //Directory.CreateDirectory(Application.streamingAssetsPath + "/graphFiles/");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
